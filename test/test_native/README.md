Use this directory to store tests that can be run independently of the board, for datastructures and algorithms that do not need hardware specific resources

all test files must have separate directory to prevent overwriting